{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ai-share global.yaml",
  "type": "object",
  "required": [
    "model"
  ],
  "properties": {
    "model": {
      "type": "string",
      "description": "Default Codex model id from models.yaml.",
      "minLength": 1
    },
    "codex_min_version": {
      "type": "string",
      "description": "Minimum Codex CLI version checked by ai:check.",
      "minLength": 1
    },
    "codex_allow_login_shell": {
      "type": "boolean",
      "description": "Whether Codex shell commands may use login shells."
    },
    "codex_windows": {
      "type": "object",
      "properties": {
        "sandbox": {
          "type": "string",
          "description": "Windows sandbox backend.",
          "minLength": 1,
          "enum": [
            "elevated",
            "unelevated"
          ]
        }
      }
    },
    "codex_shell_environment_policy": {
      "type": "object",
      "properties": {
        "inherit": {
          "type": "string",
          "description": "Inherited environment policy for shell commands.",
          "minLength": 1,
          "enum": [
            "all",
            "core",
            "none"
          ]
        },
        "ignore_default_excludes": {
          "type": "boolean",
          "description": "Whether to ignore Codex default env excludes."
        },
        "experimental_use_profile": {
          "type": "boolean",
          "description": "Whether to read shell profile files for command environments."
        },
        "exclude": {
          "type": "array",
          "description": "Environment variable patterns excluded from shell commands.",
          "items": {
            "type": "string",
            "description": "Environment variable patterns excluded from shell commands.",
            "minLength": 1
          }
        },
        "include_only": {
          "type": "array",
          "description": "Environment variable patterns included in shell commands.",
          "items": {
            "type": "string",
            "description": "Environment variable patterns included in shell commands.",
            "minLength": 1
          }
        },
        "set": {
          "type": "object",
          "additionalProperties": {
            "type": "string",
            "description": "Explicit shell command environment value.",
            "minLength": 1
          },
          "propertyNames": {
            "pattern": "^[A-Za-z_][A-Za-z0-9_]*$"
          }
        }
      }
    }
  }
}
