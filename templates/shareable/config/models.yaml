# Provider associations and defaults are declared in provider.yaml.
example-frontier:
  model_name: example-frontier
  reasoning_effort: high

example-fast:
  model_name: example-fast
  reasoning_effort: low
