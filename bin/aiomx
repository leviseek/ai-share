#!/usr/bin/env sh
set -eu

script_dir="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"

if ! command -v bun >/dev/null 2>&1; then
  printf '%s\n' "缺少 bun，无法启动 Codex + OMX。" >&2
  exit 1
fi

exec bun "$script_dir/aiomx.ts" "$@"
