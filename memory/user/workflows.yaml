# 工作流偏好 - 结构化摘要
# stable/workflows.yaml 是确认事实源；本文件只保留轻量入口。

canonical_refs:
  stable_workflows: memory/stable/workflows.yaml
  execution_contract: memory/policies/ai-execution-contract.md

runtime_preferences:
  default_process: 理解上下文 -> 制定计划 -> 实现验证 -> 代码审查
  verification_summary: 修改后运行类型检查、lint、构建和相关测试
  git_summary: 无明确用户要求不提交、不推送、不改写历史
  debugging_summary: 定位根因优先，连续失败后停止扩大修改并复盘

memory:
  write_policy: 人工审核后写入
  location: memory/
  sync: 跨设备通过 Git 同步
